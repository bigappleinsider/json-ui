export enum ComponentType {
  Image = 'image',
  Container = 'container',
  Text = 'text',
  TextField = 'textfield',
  TextArea = 'textarea',
  Button = 'button',
  Link = 'link',
  Select = 'select',
}
